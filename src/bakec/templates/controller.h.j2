/**
 * @file  {{ name }}_controller.h
 * @brief Generated controller interface for {{ model.name }} model
 *
 * {{ model.description | default("") | wordwrap(72) }}
 *
 * Generated by bakeC v{{ meta.generator_version }}
 * Model:    {{ meta.model_path }} [sha256:{{ meta.model_hash }}]
 * Platform: {{ meta.platform_path }} [sha256:{{ meta.platform_hash }}]
 * Date:     {{ meta.timestamp }}
 *
 * DO NOT EDIT — generated file. Modify model or templates instead.
 */

#ifndef {{ header_guard }}
#define {{ header_guard }}

#include "{{ name }}_controller_types.h"

/* Model input signals */
typedef struct {
{% for inp in model.inputs %}
    real_T {{ inp.name }};  /* {{ inp.description | default(inp.name) }}{% if inp.unit is defined %} [{{ inp.unit }}]{% endif %} */
{% endfor %}
} {{ name }}_ExtU_T;

/* Model output signals */
typedef struct {
{% for out in model.outputs %}
    real_T {{ out.name }};  /* {{ out.description | default(out.name) }}{% if out.unit is defined %} [{{ out.unit }}]{% endif %} */
{% endfor %}
} {{ name }}_ExtY_T;

/* Public API — lifecycle functions */
extern void {{ init_function }}(void);
extern void {{ step_function }}(void);
extern void {{ terminate_function }}(void);

/* External input/output access */
extern {{ name }}_ExtU_T {{ name }}_U;
extern {{ name }}_ExtY_T {{ name }}_Y;

#endif /* {{ header_guard }} */
